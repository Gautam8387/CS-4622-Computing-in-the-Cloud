# ./services/api-gateway/requirements.txt
Flask==2.3.2
boto3==1.26.0 # Keep if gateway needs S3 interaction (e.g. generating signed URLs, though maybe not needed now)
redis==4.5.1
celery==5.3.6 # Match celery version across services
PyJWT==2.6.0
requests==2.31.0 # Needed for calling Upload Service
python-dotenv==1.0.0