# ./services/auth-service/requirements.txt

# Core web framework
Flask>=2.3,<3.0

# OAuth library (depends on requests)
Authlib>=1.2,<1.3
requests>=2.31,<3.0

# JWT creation/validation
PyJWT>=2.6,<3.0
# python-jose is another option, choose one if needed for JWT

# Redis client (Needed because common code likely uses it)
redis>=4.5,<5.0

# Celery (Needed because common code imports/uses it)
celery>=5.3,<6.0

# AWS SDK (Needed because common/storage.py imports it)
boto3>=1.28,<1.29  # Add boto3
botocore>=1.31,<1.32 # Add botocore (boto3 dependency)

python-dotenv>=1.0,<2.0